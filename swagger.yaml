openapi: 3.0.0
info:
  title: Neunovapdf API
  version: "1.0"
servers:
  - url: http://localhost:3000
paths:
  /api/merge:
    post:
      summary: Merge PDFs
  /api/split:
    post:
      summary: Split PDF into pages
  /api/extract:
    post:
      summary: Extract pages
  /api/compress:
    post:
      summary: Compress PDF (uses ghostscript)
  /api/pdf2jpg:
    post:
      summary: Convert PDF to JPG (uses pdftoppm)
  /api/jpg2pdf:
    post:
      summary: Convert JPGs to PDF
  /api/word2pdf:
    post:
      summary: Convert Word to PDF (uses LibreOffice)
  /api/excel2pdf:
    post:
      summary: Convert Excel to PDF (uses LibreOffice)
  /api/ppt2pdf:
    post:
      summary: Convert PPT to PDF (uses LibreOffice)
  /api/pdf2word:
    post:
      summary: Convert PDF to Word (uses LibreOffice)
  /api/pdf2excel:
    post:
      summary: Convert PDF to Excel (uses LibreOffice)
  /api/pdf2ppt:
    post:
      summary: Convert PDF to PPT (uses LibreOffice)
  /api/protect:
    post:
      summary: Protect PDF (uses qpdf)
  /api/unlock:
    post:
      summary: Unlock PDF (uses qpdf)
